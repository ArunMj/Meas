
uasge :


python meas.py

-----------------------------------------------
usage: meas [-h] [-p PORT] [-s HOST] [-v] [-l LOGFILE]

optional arguments:
  -h, --help            show this help message and exit
  -p PORT, --port PORT  Port to listen
  -s HOST, --host HOST  host to bind
  -v, --version         Show version and exit.
  -l LOGFILE, --log LOGFILE
                        location of log file



* edit 'conf/meas.json'  with required mail configuration

* for editing mail templates,
	edit html jinja2 template files at 'core/email_templates'
